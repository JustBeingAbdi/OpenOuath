
let axios = require("axios");
let config = require("./config");


exports.GetGithubAuthURL = async(scope, callback) => {
    axios({
        method: "GET", 
        url: config.url,
        
        data: {
            scope: scope,
            callback: callback
        }

    }).then(async(response) => {
    });
}